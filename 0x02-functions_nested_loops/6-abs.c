#include "main.h"

/**
* _abs - Returns the decision
* @n: The value to decide on
*
* Return: It always returns success as zero
*/

int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n == 0)
{
return (n);
}
else
{
return (n * -1);
}
}
