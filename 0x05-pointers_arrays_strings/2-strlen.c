#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - check the code
 *
 * @s: The first integer
 *
 * Return:  The number of characters in a string
 */
int _strlen(char *s)
{
return (strlen(s));
}
